import {createStitches} from '@stitches/react';

export const {styled} = createStitches({
theme:{
    colors:{
        accentColour:'yellow',
        bgOne:"green"
    },
    fontSizes:{
        100: "14px"
    },
    fontWeights:{
        400:"400"
    }
}
});